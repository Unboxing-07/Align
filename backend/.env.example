# Server
PORT=8080

# Database (managed by docker-compose)
DATABASE_URL="postgresql://align:align123@db:5432/align"

# JWT
JWT_SECRET="dev-secret-key-change-in-production"
JWT_EXPIRES_IN="7d"

# CORS
FRONTEND_URL="http://localhost:3000"

# Gemini API (for AI-powered workflow generation)
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY="your-gemini-api-key-here"

# Use real AI for workflow generation (default: false, uses mock data)
# Set to "true" to use Gemini API (note: free tier is limited to 1 request per day)
USE_REAL_AI=true
